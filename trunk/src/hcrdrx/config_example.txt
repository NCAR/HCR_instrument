# Example config file for HCR
start_on_1pps           1
radar_id                        HCR
gates                   		200

# Calibration file. Note that there is normally a separate calibration
# file for each transmit pulse width.
calibration_file                /home/hcr/projDir/mgen/params/hcr_cal.xml

gates                           500
staggered_prt                   0
prt1                            1.01376e-4  # s

digitizer_gate0_delay			0.0         # s
digitizer_sample_width			2.56e-7		# sample time per digitized gate, s

# NOTE: if you change tx_pulse_width, you likely need to change 
# calibration_file as well!
tx_pulse_width                  2.56e-7     # s
tx_pulse_mod_delay              4.32e-7		# s
tx_pulse_mod_width              7.84e-7     # s
tx_delay                        3.36e-7		# s

# Measured latency between tx pulse signal generated by the Pentek timer and 
# the actual RF pulse from the transmitter.

tx_latency                      6.00e-7		# s

# iqcount_scale_for_mw: count scaling factor to easily get power in mW from
# I and Q.  If I and Q are counts from the Pentek, the power at the A/D in 
# mW is:
#
#      (I / iqcount_scale_for_mw)^2 + (Q / iqcount_scale_for_mw)^2
#
# This value is determined empirically.

iqcount_scale_for_mw            9465

# FL1 location
latitude            			40.03794
longitude           			-105.24127
altitude            			1609

# size of queue which acts as buffer for the merge

merge_queue_size				20000	# pulses

# IWRF data export

iwrf_server_tcp_port                12000   # port number
pulse_interval_per_iwrf_meta_data	5000    # pulses

# Enable/disable simulated PMC-730 card

simulate_pmc730         1

# simulation of antenna angles

simulate_antenna_angles 1
sim_n_elev              1
sim_start_elev          90.0    # deg
sim_delta_elev          0.1     # deg
sim_az_rate             6.0     # deg/s
