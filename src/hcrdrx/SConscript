# -*- python -*-

tools = Split("""
profilerdds
pentek
qtt_qtconfig
boost_date_time
boost_program_options
doxygen
""")

env = Environment(tools = ['default'] + tools)

p = env.Program('profilerdrx', 'main.cpp')
Default(p)

